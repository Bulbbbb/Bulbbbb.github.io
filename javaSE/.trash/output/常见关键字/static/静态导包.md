---
aliases: 
tags: 
---
_为了防止编程时包名过程，Java 中引入了 import 关键词。import 关键字只能导入类。因此 Java 中增加了一种静态导包的语法，可以导入指定类的某个静态成员。_

```java
import static ...
```

+ _使用 import 时可以省略包名，使用 import static 可以省略类名。_
+ _import 和 import static 没有顺序要求。_
